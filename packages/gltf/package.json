{
  "name": "@retro-engine/gltf",
  "version": "0.0.0",
  "description": "In-house glTF 2.0 / GLB parser for Retro Engine. GLB container reader, glTF JSON schema types, and accessor decoder — no runtime parsing dependency.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@retro-engine/ecs": "workspace:*",
    "@retro-engine/engine": "workspace:*",
    "@retro-engine/math": "workspace:*",
    "@retro-engine/renderer-core": "workspace:*"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "publishConfig": {
    "access": "restricted",
    "registry": "https://npm.pkg.github.com",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "oxlint .",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "bench": "bun run bench/index.ts"
  }
}
