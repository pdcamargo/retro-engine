{
  "name": "@retro-engine/engine",
  "version": "0.0.0",
  "description": "Retro Engine — App, plugins, schedules. The surface a game targets.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "publishConfig": {
    "access": "restricted",
    "registry": "https://npm.pkg.github.com",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "oxlint .",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "bench": "bun run bench/index.ts"
  },
  "dependencies": {
    "@retro-engine/assets": "workspace:*",
    "@retro-engine/ecs": "workspace:*",
    "@retro-engine/math": "workspace:*",
    "@retro-engine/renderer-core": "workspace:*"
  }
}
