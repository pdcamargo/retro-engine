name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  verify:
    name: Lint + Typecheck + Test + Build + Bench
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Restore Turbo cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-${{ github.job }}-${{ github.ref_name }}-${{ github.sha }}
          restore-keys: |
            turbo-${{ github.job }}-${{ github.ref_name }}-
            turbo-${{ github.job }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bunx turbo run lint

      - name: Typecheck
        run: bunx turbo run typecheck

      - name: Test
        run: bunx turbo run test

      - name: Build
        run: bunx turbo run build

      - name: Bench
        # Runs every bench end-to-end; catches compile / runtime drift in
        # bench files. Does NOT compare against committed baselines — see
        # CLAUDE.md §11 and ADR-0017 §5 for why CI doesn't gate on perf
        # numbers (`bench:check` stays a local, pre-PR step).
        run: bunx turbo run bench

      - name: Changeset status
        # Warning-only during pre-0.1.0; flip `continue-on-error` to false after first publish.
        continue-on-error: true
        run: bunx changeset status --since=origin/main
